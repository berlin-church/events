/*
 * TobBar Messages
 *
 * This contains all the text for the TobBar component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TobBar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TobBar component!',
  },
});
