/*
 * LayoutWrapper Messages
 *
 * This contains all the text for the LayoutWrapper component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LayoutWrapper';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: '',
  },
});
